import { PlatformFilterPipe } from './platform-filter.pipe';

describe('PlatformFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
